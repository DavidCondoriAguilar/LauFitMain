package com.laufitness.ui.theme

import androidx.compose.ui.graphics.Color

val Red = Color(0xFFE53935)        // Rojo intenso
val Black = Color(0xFF000000)      // Negro
val White = Color(0xFFFFFFFF)      // Blanco

// Colores adicionales
val DarkGray = Color(0xFF212121)   // Gris oscuro, para contraste
val LightGray = Color(0xFFBDBDBD)  // Gris claro, para fondos

// Colores de acento
val RedLight = Color(0xFFEF5350)   // Rojo claro para acentos
val RedDark = Color(0xFFC62828)    // Rojo oscuro para contrastes

// Colores para detalles y énfasis
val WhiteGray = Color(0xFFF5F5F5) // Gris muy claro para fondos